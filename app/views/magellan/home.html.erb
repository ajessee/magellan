<% provide(:title, "Welcome") %>
<%= javascript_pack_tag 'nav_scroll' %>
<div class="">
  <!-- Hero -->
  <%= render 'hero' %>

  <!-- How it works -->
  <%= render 'how_it_works' %>

  <!-- FAQ -->
  <%= render 'faq' %>

  <!-- Testimonials -->
  <%= render 'testimonials' %>

  <!-- Contact -->
  <%= render 'contact' %>

  <!-- Footer -->
  <%= render 'footer' %>

</div>



